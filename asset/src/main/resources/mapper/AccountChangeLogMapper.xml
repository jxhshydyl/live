<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ex.asset.mapper.AccountChangeLogMapper">
    <sql id="tableName">
        account_change_log
    </sql>

    <sql id="baseColumn">
        id,user_id,account_id,opponent_account_id,asset_id,type,order_id,amount,new_balance,create_time
    </sql>

</mapper>